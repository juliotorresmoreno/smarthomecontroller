<p id="notice"><%= notice %></p>

<h1>Parameters</h1>

<table class="table">
  <thead>
    <tr>
      <th>Parameter</th>
      <th>State</th>
      <th>Name</th>
      <th>Created at</th>
      <th>Update at</th>
      <th>Delete at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parameters.each do |parameter| %>
      <tr>
        <td><%= parameter.parameter_id %></td>
        <td><%= parameter.state %></td>
        <td><%= parameter.name %></td>
        <td><%= parameter.created_at %></td>
        <td><%= parameter.update_at %></td>
        <td><%= parameter.delete_at %></td>
        <td><%= link_to 'Show', parameter %></td>
        <td><%= link_to 'Edit', edit_parameter_path(parameter) %></td>
        <td><%= link_to 'Destroy', parameter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Parameter', new_parameter_path %>
