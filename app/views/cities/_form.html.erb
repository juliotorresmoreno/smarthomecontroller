<%= form_for(city) do |f| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label for="city_id">ID</label>
    <input id="city_id" type="number" class="form-control" name="city[city_id]" readonly>
  </div>

  <div class="field">
    <label for="city_name">Nombre</label>
    <input id="city_name" type="text" class="form-control" name="city[name]" required>
  </div>

  <div class="field">
    <label for="city_state_id">Departamento</label>
    <input id="city_state_id" type="number" class="form-control" name="city[state_id]" required>
  </div>

  <div class="actions">
    <%= f.submit 'submit', :class => 'btn btn-success' %>
  </div>
<% end %>
