<p id="notice"><%= notice %></p>

<h1>Perfils</h1>

<table class="table">
  <thead>
    <tr>
      <th>Perfil</th>
      
      <th>Name</th>
      <th>Creado</th>
      <th>Modificado</th>
      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @perfils.each do |perfil| %>
      <tr>
        <td><%= perfil.perfil_id %></td>
        <td><%= perfil.state %></td>
        <td><%= perfil.name %></td>
        <td><%= perfil.created_at %></td>
        <td><%= perfil.update_at %></td>
        
        <td><%= link_to 'Mostrar', perfil %></td>
        <td><%= link_to 'Editar', edit_perfil_path(perfil) %></td>
        <td><%= link_to 'Eliminar', perfil, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Perfil', new_perfil_path %>
