<%= form_for(petition) do |f| %>
  <% if petition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(petition.errors.count, "error") %> prohibited this petition from being saved:</h2>

      <ul>
      <% petition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :petition_id, "ID" %>
    <%= f.number_field :petition_id, class: "form-control", readonly: true %>
  </div>

  <div class="field">
    <%= f.label :petition_type_id, "Tipo" %>
    <%= f.select :petition_type_id, options_for_select(PetitionType.all), {}, class:"form-control" %>
  </div>

  <div class="field">
    <%= f.label :title, "Titulo" %>
    <%= f.text_field :title, :class => "form-control" %>
  </div>

  <div class="field">
    <%= f.label :description, "Descripción" %>
    <%= f.text_area :description, :class => "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit 'submit', :class => 'btn btn-success' %>
  </div>
<% end %>
