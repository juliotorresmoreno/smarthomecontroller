<%= form_for(petition_type) do |f| %>
  <% if petition_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(petition_type.errors.count, "error") %> prohibited this petition_type from being saved:</h2>

      <ul>
      <% petition_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :petition_type_id, "ID" %>
    <%= f.number_field :petition_type_id, class: "form-control", readonly: true %>
  </div>

  <div class="field">
    <%= f.label :name, "Nombre" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit 'submit', :class => 'btn btn-success' %>
  </div>
<% end %>
